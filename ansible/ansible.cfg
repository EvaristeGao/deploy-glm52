# deploy-glm52 — Ansible 配置
# 唯一必要项：让各 playbook（playbooks/）能发现自定义 filter 插件。
#   Ansible core 2.21 的 filter 自动发现只查 playbook 同级 filter_plugins/
#   （playbooks/filter_plugins），不会向上找 filter_plugins，
#   因此显式声明插件目录（相对本文件所在目录 ansible/）。
[defaults]
filter_plugins = filter_plugins