[project]
name = "deploy-glm52"
version = "0.1.0"
description = "GLM-5.2 P/D 分离一键部署（单一 config.yaml + SSH 编排）"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
